# 代码块测试

这是一段带有复制按钮和行号的代码测试：

```python linenums="1"
def hello_v_blog():
    # 点击右上角的按钮即可复制这段代码
    print("欢迎来到小V博客！")
    print("这里的复制按钮非常好用。")

hello_v_blog()
```

再测试一个没有行号的极简代码块：

```bash
pip install mkdocs-material
```
=== "Python 版本"

    ```python
    def hello():
        print("这是 Python 版本的代码")
    ```

=== "JavaScript 版本"

    ```javascript
    function hello() {
      console.log("这是 JS 版本的代码");
    }
    ```